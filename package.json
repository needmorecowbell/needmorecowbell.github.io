{
  "name": "needmorecowbell-blog",
  "version": "1.0.0",
  "description": "Personal blog with Hugo, GLightbox galleries, and automated UI testing",
  "private": true,
  "scripts": {
    "dev": "hugo server -D --port 1313",
    "build": "hugo --minify",
    "test:ui": "playwright test",
    "test:ui:headed": "playwright test --headed",
    "test:ui:report": "playwright show-report",
    "test:ui:debug": "playwright test --debug",
    "test:ui:update-snapshots": "playwright test --update-snapshots",
    "test:ui:docker": "docker-compose -f docker-compose.test.yml up --build --abort-on-container-exit",
    "test:ui:docker:down": "docker-compose -f docker-compose.test.yml down -v"
  },
  "keywords": [
    "hugo",
    "blog",
    "photography",
    "glightbox",
    "playwright"
  ],
  "author": "needmorecowbell",
  "license": "MIT",
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "@playwright/test": "^1.57.0"
  }
}
