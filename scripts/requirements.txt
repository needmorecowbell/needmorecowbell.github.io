# Obsidian to Hugo Publishing Tool Dependencies
# Install with: pip install -r requirements.txt

# YAML parsing for frontmatter extraction
pyyaml>=6.0

# Beautiful CLI output formatting
rich>=13.0

# MinIO object storage client for media uploads
minio>=7.2.0

# Load environment variables from .env files
python-dotenv>=1.0.0
