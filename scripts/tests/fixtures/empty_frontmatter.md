---
---

# Post With Empty Frontmatter

This post has an empty frontmatter block (just the delimiters with nothing inside).

The parser should handle this edge case gracefully.

## Content

Some regular content here that should still be parsed as the body.
